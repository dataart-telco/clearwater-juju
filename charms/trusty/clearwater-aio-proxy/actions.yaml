create-user:
  description: Create a user.
  params:
    number:
      description: The first number to provision
      type: string
    password:
      description: The first number's password
      type: string
  required: [number, password]
  additionalProperties: false
delete-user:
  description: Delete a user.
  params:
    number:
      description: The first number to provision
      type: string
  required: [number]
  additionalProperties: false
